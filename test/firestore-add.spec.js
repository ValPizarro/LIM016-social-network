import {savePost} from '../src/firebase/firestore/firestore-add.js';

jest.mock('../src/firebase/configuraciones.js');

describe('savePost', () => {
  it('Agrega el post', () => {

  });
  it('No agrega el post', () => {

  });
});
